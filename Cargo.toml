# [profile.release]
# panic = 'unwind'

[workspace]
members = [
    'node',
    # 'pallets/template',
    'pallets/account-service',
    'frame/balances',
    'consensus',
    'consensus/primitives',
    'runtime',
    'runtime/galois',
    'runtime/mathchain',
]
exclude = ["vendor"]
